kind = "web"
previewPath = "/"
title = "GhostAgent"
version = "1.0.0"
id = "artifacts/ghost-agent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20923

[services.development]
run = "pnpm --filter @workspace/ghost-agent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ghost-agent", "run", "build" ]
publicDir = "artifacts/ghost-agent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20923"
BASE_PATH = "/"
